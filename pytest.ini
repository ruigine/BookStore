[pytest]
pythonpath = backend
testpaths =
    backend/books/tests
addopts = -q --cov=books --cov-report=term-missing --cov-fail-under=100
markers =
    unit: fast, isolated tests
    integration: hits Flask app + DB
    e2e: cross-service tests with docker-compose