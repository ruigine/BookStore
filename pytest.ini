[pytest]
pythonpath = backend
testpaths =
    backend/books/tests
    backend/users/tests
markers =
    unit: fast, isolated tests
    integration: hits Flask app + DB
    e2e: cross-service tests with docker-compose